name: Develop pipeline

on:
  push:
    branches: ['develop-*']
  pull_request:
    branches: ['develop-*']

jobs:
  run-static-checking:
    uses: ./.github/workflows/static-checking.reusable.yml
  run-tests:
    # needs: run-static-checking
    uses: ./.github/workflows/run-tests.reusable.yml
